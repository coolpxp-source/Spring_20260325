<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ProductMapper">
	<!-- 여러개 검색 -->
    <select id="selectOrderList" parameterType="hashmap" resultType="com.example.demo.model.Product">
    	SELECT 
			P.PRODUCT_ID, 
			P.PRODUCT_NAME ,
		    O.ORDER_PRICE AS PRICE,
			O.ORDER_QTY AS STOCKQTY,
			ROUND(SUM(ORDER_QTY * ORDER_PRICE)) AS TOTAL
		FROM PRODUCT P
		INNER JOIN PRODUCT_ORDERS O ON P.PRODUCT_ID = O.PRODUCT_ID
		GROUP BY P.PRODUCT_ID, PRODUCT_NAME, O.ORDER_PRICE, O.ORDER_QTY
    </select>
    
</mapper>